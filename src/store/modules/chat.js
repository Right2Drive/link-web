// Initial state
/*
  userId: string,
*/
const initialState = {
  isGroup: false,
  id: 'a8sdf8ads7f87adf'
}

// Action types

// Action creators
export const chatActions = {

}

// Reducer
export function chatReducer (state = initialState, action) {
  switch (action.type) {
    default: {
      return state
    }
  }
}
